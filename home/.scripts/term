#!/bin/sh

if [ ! -z "$1" ]; then
    tmux neww "$1"
else
    [ "$DISPLAY" != "" ] && vte="termite -e" || vte="sh -c"
    $vte "sh -c 'if tmux -q has-session; then
        tmux attach-session -d
    else
        tmux new-session -n$USER -s$USER@$HOSTNAME
    fi'"
fi
