#!/bin/bash
cmd=""
[ "$DISPLAY" != "" ] && cmd="urxvt -e"
$cmd bash -c "tmux -q has-session && exec tmux attach-session -d || exec tmux new-session -n$USER -s$USER@$HOSTNAME"
if [ ! -z "$1" ]; then
    tmux neww "$1"
fi
